export interface filterModel {
  search: string | null,
  categorySelect: string | number | null,
  fromDate: string | null,
  toDate: string | null,
  sortColumn: string | null,
  sortorder: string | null
}
